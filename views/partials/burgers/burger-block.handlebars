<li>

	{{#if devoured}}
	{{burger_name}}
<button class="change-burger" data-id="{{id}}" id="name_burger" data-newburger="{{devoured}}">
		change burger!
	</button>
	{{else}}
	{{burger_name}}
	
	{{/if}}

</li>